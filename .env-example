# Base de l'API Django, SANS le suffixe « /apps » (ajoute par src/lib/network/api.ts).
# Production :
NEXT_PUBLIC_BACKEND_BASE_URL=https://bibliotech.cd/api
# Developpement local :
# NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:8000/api
